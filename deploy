REV=`git describe --always`
rm -rf _public
git clone git@github.com:ocftw/OpenGovReport14-16 --depth 1 -b gh-pages _public
cd _public
git rm -r *
cp -R ../static/ ./
git add *
git commit -m "regen for $REV" 
git push
rm -rf .git/
git init
git add *
git commit -m "deploy"
git remote add middle2 git@git.middle2.com:kinmen-chi-308579
git push -f -u middle2 master
